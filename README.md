# enterprise-rag-agent
I built a basic RAG system where documents are indexed using FAISS and queried through a FastAPI endpoint. Due to API quota limits I used local embeddings, but the architecture is production-ready and can be swapped with Azure OpenAI easily
